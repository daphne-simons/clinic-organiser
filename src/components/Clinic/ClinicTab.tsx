import type { View } from "./Layout"

interface IProps {
  view: View
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function ClinicTab(props: IProps) {
  return (
    <div>
      <h1>Clinic</h1>
    </div>
  )
}